# 构建产物，不进仓库
dist/
.astro/

# 依赖
node_modules/

# 测试产物
coverage/
test-results/
playwright-report/
blob-report/
playwright/.cache/

# 编辑器与系统
.DS_Store
.vscode/
.idea/

# 环境变量
.env
.env.production

# 注意：src/content/ 是 npm run sync 的生成目录，但需要提交，
# 因为 GitHub Actions 里构建前会先跑 sync 重新生成，提交它只是为了
# 让本地未跑 sync 时 astro dev 也能直接工作。不要把它加进本文件。
